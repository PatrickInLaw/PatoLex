"""OCR acquisition + consensus + workers. Future repo seam: 'OCR toolset'. Takes raw scans -> consensus
text; does NOT import the correction pipeline (the correction pipeline consumes OCR output as data)."""
