"""PatoLex-specific dictionary-building inputs for the ocrcorrect engine: builds dict_additions.txt from
the gazetteer + validated legal vocab. The CA dictionary/name layer the engine loads (would be the
per-corpus pluggable config when extracting ocrcorrect/ as a standalone repo)."""
